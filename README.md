# school-seminars-bw

This project is for a close friend to display all schools that he can attend to. 


## How to run

`Podman/docker compose up --build`

Automatically runs a container, which listens to changes in its framework. 

Backend is `FastAPI`  
Frontend is `React` 

