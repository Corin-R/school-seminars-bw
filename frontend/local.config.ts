const config = {
  VITE_PORT : 8080,
  VITE_BACKEND_HOST : "host.docker.internal",
  VITE_BACKEND_PORT : "8000",
  VITE_API_BASE_URL : "https://api.schools.horizonglade.com",
};

export default config;